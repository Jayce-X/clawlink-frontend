#!/usr/bin/expect -f
set timeout 30
spawn ssh -o StrictHostKeyChecking=no -p 51022 root@74.211.111.210
expect "password:"
send "rOqSPYoBeibQ8XoLcNOA\r"
expect "# "
send "nginx -T 2>/dev/null | grep -A 5 'listen 80\\|listen 443\\|server_name.*clawlink' | head -40 && echo __DONE__\r"
expect "__DONE__"
expect "# "
send "exit\r"
expect eof
